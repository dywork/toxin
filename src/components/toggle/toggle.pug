mixin toggle({ text, isChecked })
  .toggle
    .toggle__wrap
      label.toggle__label
        input.toggle__input(type='checkbox', checked=isChecked)
        span.toggle__label-text(tabindex='0')= text
